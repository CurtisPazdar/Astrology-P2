<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
<link rel="stylesheet" href="../public/assets/style.css">

<h1 style="font-size: 30px">Horoscope Time!!</h1>
<form action="/logout?_method=DELETE" method="POST">

</form>
<h1 style="font-size: 30px;">What's your sign?!</h1>



<ul>
    {{#each horoscopes}}
    {{#unless daily}}
    {{> horoscopes/horoscope-block daily=true}}
    {{/unless}}
    {{/each}}
</ul>

<ul>
    {{#each horoscopes}}
    {{#unless daily}}
    {{> horoscopes/horoscope-block daily=false}}
    {{/unless}}
    {{/each}}
</ul>

<body>

    <h2 style="font-size: 30px">Enter your Zodiac sign below to receive your daily horoscope:</h2>
    <nav>
        <div style="white" class="nav-wrapper">
            <form>
                <div class="input-field">
                    <input id="search" type="search" required>
                    <label class="label-icon" for="search"><i class="material-icons"></i></label>
                    <i class="material-icons">search</i>
                </div>
            </form>
        </div>
    </nav>
    <button style="color:white" type="logout">Logout</button>
    <a href="../views/index.handlebars"></a>

    <script type="text/javascript">
        fetch("https://json.astrologyapi.com/v1/sun_sign_prediction/daily/:zodiacName", {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                horoscope: 'url',
            })
        }).then(res => {
            return res.json()
        }).then(data => console.log(data))
            .catch(error => console.log('ERROR'))
    </script>
    <a href="../public/assets/horoscope.js"></a>
</body>